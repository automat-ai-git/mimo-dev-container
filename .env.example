# =============================================================================
# Docker GID — GID группы docker на хосте (для доступа к docker.sock)
# Узнать:  stat -c '%g' /var/run/docker.sock
# =============================================================================
DOCKER_GID=999
